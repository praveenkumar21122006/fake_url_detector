# Copy this file to .env and adjust as needed.

# Port the Express server listens on.
PORT=3000

# development | production
NODE_ENV=development

# Where scan history (hostname + score only, no full URLs) is persisted.
DATA_DIR=./data
